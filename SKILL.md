---
name: flight-hotel-search
description: "查即時機票票價與飯店房價（資料來源 Google Flights / Google Hotels），並比較訂購通路是航空公司官網還是 OTA。當使用者想知道某段航線多少錢、某個城市住宿行情、哪天出發最便宜、某間飯店現在什麼價位時觸發。涵蓋各種句型——直述（『查一下台北飛東京的機票』、『京都的飯店大概多少』）、比較（『十月哪天飛最便宜』、『跨年住東京會貴多少』、『官網比較便宜還是 Agoda』）、規劃口吻（『我想十一月去京都，住宿抓多少預算』、『幫我看看櫻花季的房價』、『規劃三月大阪五天四夜，機票加住宿大概要抓多少預算』）、追問（『那換成羽田呢』、『住四晚呢』）。即使使用者沒說『查機票』『查飯店』這些字，只要意圖是想知道**現在實際要花多少錢**，就該觸發。**與行程紀錄查詢的分界**：問『我行程裡訂了哪間飯店 / Day 5 的班機幾點』是查既有的行程紀錄，屬於管理行程文件的 skill；問『這段航線現在多少錢 / 還有哪些選擇』是查即時行情，用本 skill。若要把查到的結果寫進行程文件，先用本 skill 查、整理好格式，再交給負責寫入的 skill。"
---

# Flight & Hotel Search — 查即時票價與房價


從 Google Flights / Google Hotels 抓即時價格。核心洞察是：**Google 把首屏結果 server-side render 進 HTML，所以大多數查詢用 `curl` 就夠，不必開瀏覽器**——快 20 倍。只有「這個價格是誰在賣」那一層是動態載入的，才需要瀏覽器。

先判斷使用者要哪一種，別預設全都要跑：

| 使用者想知道 | 用哪個 | 耗時 |
|---|---|---|
| 這段航線/這個城市現在多少錢 | 單次查詢（`gflight.sh` / `ghotel.sh`） | ~1 秒 |
| 哪天出發/入住最便宜 | 日期掃描（`gscan.py`） | 每 10 天約 3 秒 |
| 官網比較便宜還是 OTA？會不會是分段購票 | 瀏覽器抓訂購來源（見 references） | ~15 秒 |

腳本都在 `scripts/`，用絕對路徑呼叫。各語系的解析規則集中在 `scripts/locales.py`。輸出已排序、已格式化，直接轉述給使用者即可，不需要再包一層自己的表格。

## 單次查詢

```bash
scripts/gflight.sh 臺北市 東京都 2026-10-01 2026-10-05   # 來回（城市名，涵蓋所有機場）
scripts/gflight.sh TPE KIX 2026-11-15                    # 單程（省略回程日，鎖定特定機場用代碼）
scripts/gflight.sh 臺北市 大阪 2026-03-15 2026-03-20 長榮航空   # 指定航空（中文名，一次一家）
python3 scripts/gnolcc.py 臺北市 大阪 2026-03-15 2026-03-20    # 排除廉航（見下方陷阱）
scripts/ghotel.sh 京都 2026-11-24 2026-11-26 2           # 地點 入住 退房 成人數
```

**日期只給月/日、沒給年份時，先自己換算成年份再呼叫腳本**：使用者說「2月初」「12月底」這種話，年份要用「今天算起，下一個還沒過的那個 2 月/12 月」，不是機械地套當年——8 月講「2月初」十之八九是明年。腳本對過去日期會直接報錯擋下，但那是最後一道防線，不要依賴它來發現算錯了；動筆前先確認一次比較快。

機場代碼可以用城市碼涵蓋多個機場：`TYO` = 成田＋羽田，`OSA` = 關西＋伊丹。想鎖單一機場就直接給 `NRT` / `HND` / `KIX`。

**但台北是個例外，容易漏資料**：`TPE` 只涵蓋桃園，不含松山（TSA）。實測台北→東京這段，用 `TPE` 查只回 24 筆，改用城市名「臺北市」查回 32 筆——多出來的 8 筆是松山出發、中華/長榮/全日空聯營的航班，時段通常比桃園班次更好（市區到松山近，不用大老遠跑桃園）。`gflight.sh` 的兩個參數本來就是直接塞進查詢字串，所以**直接給中文城市名（臺北市、東京都、大阪）就行，不必費心找機場碼**；只有使用者明確要鎖特定機場時才用代碼。飯店的地點也是同樣道理，直接給中文地名即可，不需要查任何 ID。

**人數只能算近似值**：Google Flights 的文字查詢介面不吃乘客數參數，不管 `q=` 裡怎麼寫「for 2 adults」都無效，回傳的永遠是單人來回價。所以多人時要自己把單人價乘以人數，並在回報時講清楚「這是單人價換算，不是系統直接算出的多人總價」——已經實測驗證這個換算在同起降點的情況下數字對得上（同一航段用瀏覽器直接輸入 2 位成人查到的總價，跟單人價 ×2 一致），但兒童票、敬老票這類非等比的票種它不會反映出來。

回報時，除了價格本身，這幾件事對使用者的決策更關鍵，看到了就主動講：

- **飛行時間與轉機**：便宜 2,000 元但多轉一趟、多花 6 小時，未必划算
- **紅眼班機**：凌晨 2:00 出發那種，價格漂亮但等於少一天行程又睡不好
- **抵達機場**：TPE→TYO 最便宜的常是 NRT，但成田進市區比羽田多一小時車程和車錢
- **飯店的評分與評論數**：4.5★ 但只有 30 則評論，參考價值遠低於 4.1★ 有 2,000 則
- **同期有沒有節慶、連假、活動**：某天特別貴或特別便宜，常常有具體原因（國定假日、賞楓/賞櫻旺季、煙火大會、限定點燈）。查得到的話用 WebSearch 確認並附上來源連結，比只丟一句「這天比較貴」有用得多——使用者能判斷這個貴是不是他在意的理由（想看點燈就值得，純粹撞連假就該閃）。

## 語系與幣別

預設 **zh-TW / TWD / tw**，跟加入多語支援前完全一樣，既有用法不受影響。要換語系就設環境變數：

```bash
GFH_LANG=en scripts/gflight.sh Taipei Tokyo 2026-10-01 2026-10-05   # 英文介面、USD
GFH_LANG=ja scripts/ghotel.sh 金沢 2026-11-24 2026-11-26 2          # 日文介面、JPY
GFH_LANG=zh-TW GFH_CURR=JPY scripts/ghotel.sh 京都 2026-11-24 2026-11-26 2  # 中文介面看日圓報價
```

| 變數 | 預設 | 說明 |
|---|---|---|
| `GFH_LANG` | `zh-TW` | 目前支援 `zh-TW` / `en` / `ja` |
| `GFH_CURR` | 跟著語系（TWD/USD/JPY） | 可單獨覆寫，語系與幣別互相獨立 |
| `GFH_REGION` | 跟著語系（tw/us/jp） | Google 的 `gl` 參數 |
| `GFH_UA` | 一般瀏覽器字串 | 要改用自訂識別時設定 |

**預設不要主動切換語系。** 使用者讀的是中文，回報日文或英文的飯店名只會增加他的負擔——
即使那樣拿到的資料比較完整。只有這兩種情況才換：

1. **使用者明確要求**（「用日文查」「幫我看英文報價多少」）
2. **中文介面查不到**目標飯店，或使用者要找的是日本在地小型業者——這時用 `ja` 補查，
   但**回報時務必附上中文對照**，否則使用者拿著日文店名去訂房會找不到

**別自己翻譯航空公司或飯店名**——腳本回什麼就轉述什麼，需要對照時同時附上原文，否則使用者拿去訂房會找不到。

各語系的解析規則在 `scripts/locales.py`。**要加新語系必須實際抓一次該語系的頁面比對 regex**，不能照文法推——推出來的 regex 會安靜地回空清單，跟「地名打錯」「被限流」長得一模一樣。

## 日期掃描

使用者說「哪天最便宜」「幾月去比較划算」「避開貴的日子」時用這個。

```bash
python3 scripts/gscan.py flight TPE TYO 2026-10-01 2026-10-31 --nights 4
python3 scripts/gscan.py hotel 東京 2026-12-25 2027-01-05 --nights 3
```

`--nights` 是停留天數（機票）或住幾晚（飯店），掃描器會自動配對回程/退房日。

**`ghotel.sh` 每晚房價與住宿總價都會給**，兩者都含稅金與相關費用。總價是直接從頁面抓的（zh「總價為 \$12,067」、en「\$381 total」、ja「合計 ￥68,216」），不是乘出來的。**不要拿每晚房價除以晚數**（實際踩過這個錯，把每晚 NT\$2,481 誤當成 5 晚總價、再除出 NT\$496 的假每晚價，整整差了 5 倍）。

實測 4 組查詢、72 間飯店（2/3/5/7 晚），**總價一律等於每晚 × 晚數**，誤差只有四捨五入的 1–3 元——連泊折扣若存在，早就算進那個每晚均價裡了。所以「長住有折扣、總價不會是整數倍」這個直覺是錯的。parser 反過來拿這個關係當核帳條件：某筆對不上，就代表版面改過、總價配到了錯的飯店，輸出末尾會示警。這件事非查不可，因為配錯的結果是「每個數字都合法、只是屬於別間飯店」，畫面上完全看不出來。

`gscan.py` 只顯示每晚中位數——它每個日期都是獨立查詢、晚數固定，橫向比較用每晚比用總價合適。

**飯店掃描依「當日房價中位數」排序，不是最低價**——這是刻意的。最低價那筆常常是青年旅館或膠囊，某天冒出一個便宜床位會讓那天看起來最划算，但整體行情其實很貴。實測某次日期掃描：某一天的最低價是全區間第一（NT\$912），中位數卻高達 NT\$9,062，是隔週的 4 倍——那天根本是最不該去的日子。轉述時以中位數為準，最低價當補充。

掃描結果的「價差」欄位通常是最有價值的一句話：實測台北→東京一段秋季區間，選對日子差 NT\$3,818（33%）。如果價差很小，也該明講「這段期間價格平穩，日期可以照你的方便排」，省得使用者為了幾百塊硬凹行程。

## 訂購來源（官網 vs OTA）

這層 `curl` 拿不到，必須用瀏覽器。使用者問「官網比較便宜嗎」「這是誰在賣」「Agoda 有比較便宜嗎」，或**準備要下訂了**，才值得花這 15 秒。

完整操作步驟見 `references/booking-sources.md`，裡面有選航班、進訂購頁、判別官網/OTA 的 selector 與判讀方式。

判別的關鍵：Google 在航空公司官網那筆旁邊掛一個獨立的「航空公司」badge。不要用「來源名稱裡有沒有航空公司的名字」去比對——OTA 的名稱也可能含航空公司字樣。

**飯店這邊不能只看 Google 上的「官網」數字就下結論**。Google 每個通路只給一個價，但飯店官網賣的是一整組方案（早鳥、連泊折扣、含早餐、免費取消、會員價⋯⋯），實測同一間房 21 種方案、5 晚總價差近一倍。要判斷官網划不划算，得實際進官網把「顯示全部的住宿方案」點開——預設只露出「推薦」方案，而推薦方案通常不是最便宜的。比較時還要對齊房型、含不含早餐、取消條件，否則是在比不同商品。細節見 `references/booking-sources.md`。

## 使用者說「我有某某的會員」時，把整個品牌攤開

**這是一個流程轉折點，不是附註。** 平常的流程是「Google 查行情 → 挑一間 → 進那間官網」；
但使用者一旦提到會員身分，會員價、點數回饋、官網限定方案都是**跟著品牌走、不是跟著單一
分館走**，所以要先列出該品牌在目標城市的**所有分館**，再一次比完。

做法：進品牌官網的分館列表（例如 `viainn.com` 首頁），抓出該城市所有分館的 slug 與訂房
系統的 `code`（在各館頁面「ご宿泊プランはこちら」連結的 `code=` 參數裡），然後帶同一組
日期／人數逐館查會員價。

**為什麼非做不可**：實測大阪 VIA INN 共 8 間，Google 的大阪／梅田搜尋結果只出現 2 間，
而且出現的那間（梅田館 NT\$14,273／13㎡）是**全品牌最貴**的；沒出現的心斎橋館只要
NT\$11,064，而 Prime 心斎橋四ツ橋有 15㎡ 房型、會員價 NT\$12,079——比梅田館又便宜又大。
只查 Google 給的那一間，會直接推薦到最差的選項還渾然不覺。

順帶一提，查的時候會遇到滿房：訂房頁會導向 `/booking/recommender` 並明寫「未能搜到有
空房」。**要實際讀到那句話再判定滿房**，不要因為解析不到房型就假設沒空房——兩者長得一樣。

### 訂房頁是延遲載入的，不捲到底只會看到最小的房型

飯店訂房系統（tripla / VIA INN 這類）**初次渲染只吐出前 2 種房型**，而排序通常把最便宜
的單人房放最前面。頁面上那句「搜尋結果: N 件」才是真正的房型總數——**N 跟你解析到的
數量對不上，就代表還沒載完**。做法是反覆 `window.scrollTo(0, document.body.scrollHeight)`
並等 2 秒，直到「客房大小」出現次數不再增加為止。

「搜尋結果」的筆數與「客房大小」這類標籤，都是**依你載入頁面時的顯示語言而變的**——
要比對你實際抓到的那一頁上出現的字，不要寫死某一種語言的字串。

這個坑特別惡毒，因為**截斷後的資料看起來完全正常**：每間都有房型、有價格、有會員價，
只是清一色都是 12–13㎡。實測 VIA INN 大阪四間，不捲動時每間只看到 2 種單人房，捲完後
才發現梅田館有 17㎡ 大床房與 23㎡ 豪華雙床房、Prime 心斎橋四ツ橋有兩種 20㎡ 房型。
使用者嫌房間小的時候，漏掉的正好就是他要的那些。

**推論**：任何「搜尋結果 N 件」與實際解析筆數不符的頁面，都要先確認是不是還沒載完，
再開始比價。

## 陷阱（這些會讓你算出錯誤的價格卻毫無察覺）

### 飯店的 `checkin` / `checkout` 參數是假的

直覺會寫 `?q=東京飯店&checkin=2026-12-30&checkout=2027-01-03`。它**不會報錯，會回一整頁看起來完全正常的飯店和價格**——但日期被靜默忽略，回的是「明天住一晚」的行情。實測同一間飯店：假日期 NT\$1,348 vs 真跨年 4 晚 NT\$4,207，差 3.1 倍。

**人數也在 `ts=` 裡，而且編碼方式反直覺**：人數不是「某個欄位寫個數字」，而是**重複 field 1 的次數**——一位成人一組 `{1:{1:3}}`，兩位就重複兩次。腳本原本把人數塞進另一個欄位，Google 靜默忽略、一律回 2 人房價（實測 18 間飯店在 adults=1/2/4 下價格完全相同，就是這個症狀）。修正後實測 15 間有 11 間價格隨人數變動（例：某商務飯店 1 人 NT\$2,145、2 人 NT\$2,481）。**單人房價通常比雙人便宜兩三成，別拿 2 人房價當單人預算。**

**幣別也在 `ts=` 裡，而且 URL 的 `curr=` 對飯店完全無效**——實測送 `curr=USD`、`curr=JPY`
回來的價格全是 NT$，因為真正生效的是 `ts` protobuf 內的幣別欄位。`ghotel.sh` 與 `gscan.py`
已經把 `GFH_CURR` 傳進去了，走腳本就不會踩到。

正確的日期藏在 `ts=` 這個 base64 protobuf 參數裡。`scripts/gtsgen.py` 會生成它（產出與瀏覽器選日期的結果 byte-for-byte 一致），`ghotel.sh` 已經在用了——**所以只要走腳本就不會踩到**。這條寫在這裡是為了：如果你打算自己組 URL 繞過腳本，會掉進去。

推論到一般情況：**Google 收到不認識的參數不會回 400，而是安靜地用預設值算給你**。所以任何時候你自己動手組查詢參數，都要做對照組驗證——換一個「應該產生明顯差異」的輸入（跨年 vs 平常週間），如果結果沒變，那參數就沒生效。

### 「來回總價 X 起」的航空公司只代表去程

搜尋結果每一筆的資料裡**只有去程航段的航空公司**，價格是「來回總價 X 起」——那個
「起」是配上最便宜回程算出來的下限，而最便宜的回程往往是廉航。所以**拿搜尋結果過濾
航空公司，濾掉的只有去程**，回程可能是捷星、樂桃，還會變成分段購票。

使用者說「排除廉航」時走 `gnolcc.py`，不要用 `gflight.sh` 再自己挑航空公司：

```bash
python3 scripts/gnolcc.py 臺北市 大阪 2026-03-15 2026-03-20
```

原理是把航空公司名塞進查詢字串，**Google 的航空公司篩選會同時套用到兩個航段**。兩個
限制：**必須用當前語系介面的寫法**（`hl=zh-TW` 下 `China Airlines` 回 0 筆、`中華航空`
才有效；切到 `GFH_LANG=en` 則 `China Airlines` 有效——已實測），而且一次只能一家
（`中華航空 長榮航空` 並列會回 0 筆），所以腳本是逐家查再合併。

實測 TPE→KIX 一組來回日期：搜尋頁顯示「中華航空 NT\$11,711」，實際是中華去程＋捷星
回程；兩段都是中華的真實來回票是 NT\$13,819，差 NT\$2,108。這個差距足以讓整個比價結論
翻盤，而且從搜尋結果表面完全看不出來。

### 分段購票（separate tickets）

廉航組合常出現「去程 A 航空、回程 B 航空」被湊成一個便宜總價，Google 會標「分段購票·你必須逐一預訂這趟行程的 2 張機票」。這代表兩張完全獨立的票：行李不直掛、去程延誤導致回程 no-show 沒人賠、改期各算各的。

看到這個標記就要主動提醒，不要只報價格。省下的一兩千元是拿這個風險換的，而使用者往往不知道自己在承擔它。同一家航空的聯程票通常貴 30–40%，但那是保險費。

### Google 沒有房間坪數

使用者問「房間多大」時，別在 Google 的資料裡找——它根本沒這個欄位。飯店詳情頁的分頁只有「價格／詳細資訊／評論／相片／環境介紹／餐廳／熱門景點／機場」，搜尋結果 HTML 裡也沒有任何 `㎡`／`平方公尺` 字樣。要查得跳出去看**飯店官網的客房頁**，官網沒標就用 Jalan（`jalan.net/yad<ID>/room/`，客房規格表最齊）、樂天旅遊、Yahoo!トラベル 的房型欄位。實測 12 間大阪飯店全部查得到，只是來源分散：官網有標的約一半，其餘要靠 Jalan 或 Yahoo!トラベル 補齊。日文訂房站要用瀏覽器工具開，WebFetch 拿不到。

坪數對決策的影響比想像大：實測同一批大阪商務飯店，最便宜那間（NT\$1,592）是 14㎡，而貴它 30% 的兩間連鎖商務飯店只有 10㎡——價格高低跟房間大小幾乎無關，不一起查就會推薦錯。順帶一提日本商務飯店 10–12㎡ 是常態，16㎡ 以上才算寬敞。

### 超出可訂範圍時，飯店會靜默回「明天住一晚」的行情

實測邊界（2026-08 量測，臺北→大阪 / 大阪飯店）：**304 天後**機票剩 3 筆、**317 天後**剩 1 筆、**331 天後** 0 筆；飯店在 317 天仍正確回 5 晚，**345 天已經退回 1 晚**。

機票是誠實地回空清單，**飯店不是**——它回一整頁看起來完全正常的飯店與價格，只是那些數字是單晚行情。實測 2027-10-13 要求 5 晚、回的是 1 晚，而每晚房價那欄單看毫無異常，差了 5 倍卻沒有任何徵兆。

`ghotel.sh` 現在把要求的晚數傳給 parser 對帳，不符就在**表格前面**示警——印在 18 行資料後面等於沒印。`gscan.py` 則直接把該日期視同無資料：把單晚行情混進中位數排序，會排出一份看起來完全合理、實際整個錯掉的日期排行。

超過 300 天的查詢會多印一行提示。那個門檻刻意早於「完全查不到」的邊界，因為 300–330 天之間資料只是變稀疏（筆數變少但不會歸零），那段最容易把「樣本太小」誤當成「行情就是這樣」。

### Google 不等於全市場

Google 列的是有合作的 partner。廉航自家官網的限時促銷、台灣本地 OTA（可樂、東南、燦星）常常不在名單裡。所以「Google 上最便宜」≠「市場最便宜」。金額大或使用者明顯在比價時，要講這句話，別讓他以為已經看完所有選項。

### 首屏只有一頁

`curl` 拿到的是首屏 SSR 的結果——機票約 20–24 筆、飯店固定 18 間。這對「抓行情、比日期」完全夠用，但它不是全部。使用者要窮舉（例如「所有直飛的選項都列給我」）時，要說明這個限制，需要的話改用瀏覽器往下捲。

**這個限制不只影響完整性，會直接讓結論錯。** 那 18 間不是「該區最好的 18 間」，同一個
連鎖品牌常常只被抽中一兩間，而且抽中的未必便宜。實測大阪 VIA INN 共 8 間，Google 只回
2 間，其中還是最貴的那一間；真正划算的兩間（心斎橋 NT\$11,064、Prime 心斎橋四ツ橋 15㎡
NT\$12,079）從頭到尾沒出現過。

**那 18 間每次重跑都會換一批。** 同一個查詢（大阪，同一組日期）連跑三次，18 間裡有 7 間
不重疊，每晚房價中位數在 NT\$1,830／1,839／1,931 之間跳動。所以**單次查詢的中位數帶著約
5% 的抽樣雜訊**，別把它當精確的預算基準；要壓低雜訊就跑兩三次取中位數。

但輪替**不等於重跑就能補齊**。VIA INN 心斎橋那種缺漏會不會在某一次輪替中出現，尚未驗證
——會輪替的是抽樣，不保證涵蓋整個母體。要確保補齊，仍然只能從品牌官網、或換更細的地名
關鍵字（「心齋橋」「新大阪」）另外查。

## 與其他 skill 的整合

本 skill **只查詢——不寫檔，也不能訂票訂房**。使用者要下訂時，給他方向（哪個通路、大概價位、注意分段購票），讓他自己完成。

要把結果存進行程文件時，本 skill 負責查詢與整理，**實際寫檔交給負責管理那些文件的 skill**。原因是那個 skill 才懂檔案結構、格式慣例和查重邏輯，另起一套寫入會產生重複條目或格式衝突。

做法是把查詢結果整理成乾淨的候選清單（含價格、時間、評分、你的推薦與理由），明確標出哪個是建議定案、哪些是備選，然後提示使用者由對應的 skill 接手寫入。

**本機專屬的銜接設定**：若 `references/local.md` 存在就讀它——那裡記錄這台機器上實際要銜接哪個 skill、以及使用者的文件慣例。該檔案不進版本庫，所以本 skill 的公開版本不預設任何特定的 skill 生態。

## 出錯時

- **回「錯誤：⋯已經是過去的日期」**：腳本會主動擋下並直接講原因，不用再猜——通常是使用者講「幾月初/幾月」沒帶年份，而那個月今年已經過了，換算成日期時卻套了今年。改用明年重查。
- **回「(無資料)」**（沒有前述錯誤訊息）：先確認機場代碼與地名拼寫，日期格式須為 `YYYY-MM-DD`。
- **掃描時部分日期沒資料**：腳本會提示「可能是限流而非真的無航班」。並行度限制為 6（對來源網站的禮貌性節流），若仍發生就縮小日期範圍分批跑，不要直接告訴使用者「那幾天沒航班」——那多半是假的。
- **價格看起來不合理**（例如跨年比平日便宜）：這是日期沒生效的典型症狀，回去檢查是不是繞過腳本自己組了 URL。

價格是即時抓的，Google 自己註明結果為 24 小時內的資訊。回報時可以提一句實際下訂時可能已變動，但不需要每次都長篇免責——講一次就好。

本 skill 只查詢，**不能訂票訂房**。使用者要下訂時，給他方向（哪個通路、大概價位、注意分段購票），讓他自己完成。
